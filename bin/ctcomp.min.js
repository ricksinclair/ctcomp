#!/usr/bin/env node
const fs=require("fs"),path=require("path");function createComponent(e){const n=path.join(process.cwd(),e);fs.mkdirSync(n);const o=path.join(n,`${e}.tsx`),s=`import styles from './${e}.module.scss';\n\n  function ${e}() {\n    return (\n      <div className={styles.${e}}>\n        {/* Your component JSX goes here */}\n      </div>\n    );\n  }\n\n  export default ${e};\n  `;fs.writeFileSync(o,s);const t=path.join(n,`${e}.module.scss`),r=`.${e} {\n    /* Your component styles go here */\n  }\n  `;return fs.writeFileSync(t,r),e}if(require.main===module){process.argv.length<3&&(console.log("No component name supplied"),process.exit(1));const e=createComponent(process.argv[2]);console.log(`The ${e} component has been created.`)}else module.exports=createComponent;
